# docs / assets

Drop the README screenshots here so they render in the root `README.md`:

- `landing.png` — the landing page (hero + features).
- `chat.png` — the chat experience (a conversation, ideally with a quiz card / media sidebar).

Suggested capture: ~1600px wide, light **or** dark theme. Keep file sizes reasonable
(use PNG or compressed WebP).
